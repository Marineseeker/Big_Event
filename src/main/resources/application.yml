spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/big_event
    username: root
    password: Marine1235689412
  application:
    name: MarineSpring
  data:
    redis:
      host: localhost
      port: 6379

server:
  port: 8080

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret: ${JwtSecret}86400
  expiration: 86400